---
import type { buildSiteSchema } from "../../config/helpers";

interface Props {
  data: ReturnType<typeof buildSiteSchema>;
}

const { data } = Astro.props;
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(data)} />
